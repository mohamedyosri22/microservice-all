#General
server.port=8761

#Cloud
spring.config.name=discovery-server

#Eureka
eureka.instance.hostname=localhost
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false

eureka.username=${EUREKA_USERNAME:anakin}
eureka.password=${EUREKA_PASSWORD:anakin}